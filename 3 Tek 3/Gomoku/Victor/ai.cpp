/*
** EPITECH PROJECT, 2021
** ai
** File description:
** ai
*/

#include <iostream>
#include <array>
#include "ai.hpp"

// void check_winnig_pos(game &board)
// {
//     unsigned short size = board.size();
//     for (unsigned short i = 0; i < size*size; i++) {
        
//     }
// }

void ai_plays(game &board)
{
    // int size = (int)board.size();
    // std::array<short, size*size> num_map;
    // set_num_map(board, num_map, size*size);
    // check_winnig_pos(board);
    // board.turn(0, 0, 1);
    // std::cout << 1 << "," << 1 << std::endl;
}
